import numpy as np


def calc_min_edit_dist(source, target):
    med_mat = np.zeros((len(source) + 1, len(target) + 1))
    print(med_mat)
    pass


def align(source, target):
    pass


calc_min_edit_dist("intention", "execution")